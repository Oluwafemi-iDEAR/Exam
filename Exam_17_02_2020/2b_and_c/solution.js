/*What is a palindrome?

	A Palindrome is a word, number, phrase, or other sequence of characters
	which once spelled reads the same backwords as forword. E.g. 'Madam','EYE',
	'02022020'...
	*/

	for (var i = 1; i <= 100; i++) {
		if(i%2==0){
			document.write(i);
		}
	}